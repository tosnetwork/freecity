/**
 * @freecity/contracts — single source of command, event, snapshot, and
 * client-delta types. Wire schemas (zod) arrive in PR2; this package must
 * never import from apps, services, or workers.
 */

export const CONTRACTS_SCHEMA_VERSION = 1;
